# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

tasks:
  tags:
    internal: true
    cmds:
      - skopeo list-tags docker://docker.io/{{.DOCKER_NAME}} | jq -r '.Tags[]' | sort -V
