# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

tasks:
  freeze:
    internal: true
    cmds:
      - uv pip freeze

  sync:
    internal: true
    cmds:
      - uv sync --upgrade
